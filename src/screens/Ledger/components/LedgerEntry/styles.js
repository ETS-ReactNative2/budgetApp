import { StyleSheet } from 'react-native'
import { gray10 } from '../../../../styles/colors'

export default StyleSheet.create({
    row: {
        borderWidth: 1,
        borderColor: gray10,
        display: 'flex',
        flexDirection: 'row',
        height: 45,
        marginVertical: 2,
    },
})
